"""Optional extensions to the core (deterministic) Scientific Jigsaw tool.

Modules here are NOT imported by the core package. The deterministic tool runs
without them; they are opt-in add-ons that make up the extended tool.
"""
